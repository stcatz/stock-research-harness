{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/us-equity-research/outcome-request.schema.json",
  "title": "US-equity outcome command request",
  "oneOf": [
    { "$ref": "#/$defs/record" },
    { "$ref": "#/$defs/summary" },
    { "$ref": "#/$defs/history" }
  ],
  "$defs": {
    "record": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schema_version",
        "market",
        "run_id",
        "symbol",
        "horizon_trading_days",
        "observed_at",
        "available_at",
        "candidate_return",
        "benchmark_return",
        "benchmark_symbol",
        "source_url",
        "source_document_id"
      ],
      "properties": {
        "schema_version": { "const": "0.1" },
        "market": { "const": "US" },
        "run_id": { "type": "string", "minLength": 1 },
        "candidate_id": { "type": "string", "minLength": 1, "maxLength": 256 },
        "symbol": { "type": "string", "minLength": 1 },
        "horizon_trading_days": { "enum": [5, 20] },
        "observed_at": { "type": "string", "format": "date-time" },
        "available_at": { "type": "string", "format": "date-time" },
        "candidate_return": { "type": "number", "minimum": -1, "maximum": 100 },
        "benchmark_return": { "type": "number", "minimum": -1, "maximum": 100 },
        "benchmark_symbol": { "const": "SPY" },
        "source_url": { "type": "string", "format": "uri", "pattern": "^https://" },
        "source_document_id": { "type": "string", "minLength": 1 }
      }
    },
    "summary": {
      "type": "object",
      "additionalProperties": false,
      "required": ["schema_version", "market", "run_id", "evaluation_at"],
      "properties": {
        "schema_version": { "const": "0.1" },
        "market": { "const": "US" },
        "run_id": { "type": "string", "minLength": 1 },
        "evaluation_at": { "type": "string", "format": "date-time" }
      }
    },
    "history": {
      "type": "object",
      "additionalProperties": false,
      "required": ["schema_version", "market", "evaluation_at"],
      "properties": {
        "schema_version": { "const": "0.1" },
        "market": { "const": "US" },
        "evaluation_at": { "type": "string", "format": "date-time" },
        "limit": { "type": "integer", "minimum": 1, "maximum": 20, "default": 10 }
      }
    }
  }
}
