# A 股云端接管审计（2026-09-10）

范围：在现有 CN 引擎内补齐本地旁路交接；美股不改。开发分支 `codex/cn-cloud-handoff`。保留已有 HiThink 与报告相关未提交修改，不提交、不发布、不改变调度。

## 输入边界

已读用户粘贴需求与本机 `00_CODEX_TAKEOVER_PROMPT.md`。原接管包 README、01、02、03 和 manifest 尚未取得；不能声称导入完整的 13 条经验或 20 项原始回归种子。下列本地合同是从实际可见需求提炼的增量版本，不冒充缺失附件。历史市场案例均未核验。

## 实施前能力矩阵

| 能力 | 状态 | 本地证据 / 缺口 |
|---|---|---|
| A 股日线、历史指标 | 已实现且验证（离线测试） | `a_share_research/src/a_share_research/ingest/market_data.py`：不复权日线、10 交易日收益、5 日成交额；BaoStock / HiThink；不等于在线额度验证 |
| 分钟、竞价 | 没有 | 当前 provider 契约为日线，不能支持 09:35 判断 |
| 官方公告、政策 | 部分实现 | `ingest/snapshot_builder.py` 校验人工 seed；无正文自动发现/语义核验 |
| 海外同行与事件日历 | 部分实现 | 独立 US SEC 采集存在；CN 无同行映射/采集闭环；下一催化来自 seed |
| 在线采集 / 离线研究、市场隔离、PIT | 已实现且验证（离线测试） | 两市场独立 `core/snapshot.py`、`core/engine.py`、数据库与 artifact；历史重建有标签 |
| 题材发现、评分、反方审查 | 部分实现 | CN `core/engine.py` 读取维度、门槛、反方；排序是状态/角色/代码，非独立预测评分；HiThink 重复标签发现为现有未提交功能 |
| 正式报告、冻结输入、回读 | 已实现且验证 | `core/pipeline.py` 不可变 artifact、哈希、SQLite；`artifact-read` 上限 20000 字符 |
| 盘前 / 收盘同 ID、日期索引 | 没有 | runs 账本有研究记录，无预测-结果关联契约 |
| 去重、重试 | 部分实现 | run 哈希与锁；HiThink 有界重试；无交付重试队列 |
| 调度 | 部分实现 | `scripts/run_cn_daily.sh` 与 LaunchAgent 示例工作日 20:30；不是 07:00；当前用户的对应 LaunchAgent 未安装/未加载 |
| 交易日历、主机持续可用性 | 无法核实 | 工作日不等于交易日；未做无人值守验证 |
| 认证、预算、模型 | 无法核实 / 部分实现 | 当前环境仅确认 HiThink key 存在（未读取值）；预算/许可未确认；Python 研究流程不调用模型，DSH 会话模型未验证 |
| 私人交付、回执、告警 | 没有 / 无法核实 | wrapper stdout 并非私人送达回执；旧云端任务状态未核实 |

此前基线：CN 112、US 90 项 Python 测试通过；本轮修改后重新验证相关部分。

## 最小变更清单（先于实施确定）

1. 增加简短 AGENTS 入口、本地合同、经验登记与日常运行说明；缺失原附件显式登记。
2. 在 CN 现有 SQLite 增加仅追加交接记录，日期索引、内容哈希、父预测 ID；不创建第二数据库。
3. 在原 CLI 增加旁路 `premarket`、`close-review`、`journal-read`、`journal-list`：复用 run 与 artifact 校验，冻结全部候选和备选；收盘从同一记录读取原条件。
4. 无权发布、不能认定正式盘前样本：所有新入口固定 shadow，发布状态为未发布；fixture / reconstructed 显式区分。时间由程序记录，不可回填。
5. 收盘仅匹配同一 CN security、业务日的日线证据，程序计算日涨幅；语义确认、分钟判断、未来指标保持 UNKNOWN。不用日涨幅冒充策略收益。
6. 补关键失败测试并用已有真实快照做历史重建旁路；不消耗未核实预算。

## 验收边界

本轮优先解决持久化交接。完整原规格、实时输入、人工研究核验、交易日历、分钟数据、发布渠道、旧任务状态和无人值守均不能用 fixture 测试替代。验收后仍须独立批准生产切换。
